version: "2"
sql:
  - engine: "sqlite"
    schema: "migrations"
    queries: "queries"
    gen:
      go:
        package: "store"
        out: "store"
        emit_json_tags: true
        emit_interface: true
